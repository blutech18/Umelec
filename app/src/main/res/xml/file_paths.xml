<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Use unique names for each root so FileProvider registers both -->
    <external-files-path name="external_receipts" path="." />
    <files-path name="internal_receipts" path="." />
</paths>

